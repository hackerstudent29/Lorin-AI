{
  "name": "msajce-gateway-proxy",
  "version": "1.0.0",
  "description": "Node.js proxy that forwards Python RAG backend requests to Vercel AI Gateway",
  "type": "module",
  "main": "server.mjs",
  "scripts": {
    "start": "node --env-file=../.env.local server.mjs",
    "dev": "node --watch --env-file=../.env.local server.mjs"
  },
  "dependencies": {
    "ai": "^5.0.0",
    "express": "^4.18.2"
  }
}
